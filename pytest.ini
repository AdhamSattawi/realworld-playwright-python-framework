[pytest]
base_url = http://localhost:3000
addopts = -v --html=reports/report.html --self-contained-html --output=test-results --tracing=retain-on-failure --video=retain-on-failure --screenshot=only-on-failure
pythonpath = .
